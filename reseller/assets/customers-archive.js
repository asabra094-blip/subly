/* Archived customers UI extension */
let customerView='active',allCustomersCache=[];
const ac=(en,ar)=>window.SublyLocale?.language==='ar'?ar:en;
async function loadCustomers(){const{data,error}=await supabaseClient.from('customers').select('id,reseller_id,first_name,last_name,phone,notes,status,created_at,updated_at').eq('reseller_id',currentUser.id).in('status',['active','archived']).order('created_at',{ascending:false});if(error){console.error('[SUBLY] Customers',error);qs('customersGrid').innerHTML=`<div class="empty">${ac('Could not load customers.','تعذر تحميل العملاء.')}</div>`;return}allCustomersCache=data||[];updateCustomerTabCounts();renderCustomers()}
function updateCustomerTabCounts(){const active=allCustomersCache.filter(c=>c.status==='active').length,archived=allCustomersCache.filter(c=>c.status==='archived').length;if(qs('activeCustomerCount'))qs('activeCustomerCount').textContent=active;if(qs('archivedCustomerCount'))qs('archivedCustomerCount').textContent=archived}
function setCustomerView(view){customerView=view==='archived'?'archived':'active';qs('activeCustomersTab')?.classList.toggle('active',customerView==='active');qs('archivedCustomersTab')?.classList.toggle('active',customerView==='archived');qs('activeCustomersTab')?.setAttribute('aria-selected',String(customerView==='active'));qs('archivedCustomersTab')?.setAttribute('aria-selected',String(customerView==='archived'));if(qs('addCustomerButton'))qs('addCustomerButton').style.display=customerView==='active'?'':'none';if(qs('customerCountLabel'))qs('customerCountLabel').textContent=customerView==='active'?ac('active customers','عملاء نشطون'):ac('archived customers','عملاء مؤرشفون');renderCustomers()}
function renderCustomers(){const grid=qs('customersGrid');if(!grid)return;const q=(qs('customerSearch')?.value||'').trim().toLowerCase(),qd=digits(q);customersCache=allCustomersCache.filter(c=>c.status===customerView);const list=customersCache.filter(c=>{const name=customerFullName(c).toLowerCase(),p=digits(c.phone);return!q||name.includes(q)||String(c.phone||'').toLowerCase().includes(q)||(qd&&p.includes(qd))});if(qs('customerCount'))qs('customerCount').textContent=list.length;if(!list.length){const empty=q?ac('No customers match your search.','لا يوجد عملاء مطابقون لبحثك.'):customerView==='archived'?ac('No archived customers.','لا يوجد عملاء مؤرشفون.'):ac('No customers yet. Add your first customer.','لا يوجد عملاء بعد. أضف أول عميل.');grid.innerHTML=`<div class="empty" style="grid-column:1/-1"><div class="empty-icon">${customerView==='archived'?'🗃️':'👥'}</div>${empty}</div>`;return}grid.innerHTML=list.map(c=>{const stat=customerStats.get(c.id)||{},active=Number(stat.active_count||0),latest=stat.last_purchase,archived=customerView==='archived';return `<article class="customer-card${archived?' archived-customer-card':''}"><div class="customer-card-top"><div><div class="customer-name">${safe(customerFullName(c))}</div><a class="customer-phone" href="${safe(phoneHref(c.phone))}" target="_blank" rel="noopener noreferrer">📱 <bdi>${safe(c.phone)}</bdi></a></div><div class="customer-menu" id="menu-${c.id}"><button class="customer-menu-btn" onclick="toggleCustomerMenu('${c.id}',event)" aria-label="${ac('Customer options','خيارات العميل')}">⋯</button><div class="customer-menu-list"><button onclick="openHistory('${c.id}')">${ac('View subscriptions','عرض الاشتراكات')}</button><button onclick="openCustomerModal('${c.id}')">${ac('Edit customer','تعديل العميل')}</button>${archived?`<button class="restore" onclick="restoreCustomer('${c.id}')">${ac('Restore customer','استعادة العميل')}</button>`:`<button class="danger" onclick="deleteCustomer('${c.id}')">${ac('Archive customer','أرشفة العميل')}</button>`}</div></div></div><div class="customer-meta"><div class="customer-meta-box"><div class="customer-meta-label">${ac('Active subscriptions','الاشتراكات النشطة')}</div><div class="customer-meta-value">${active}</div></div><div class="customer-meta-box"><div class="customer-meta-label">${ac('Last purchase','آخر شراء')}</div><div class="customer-meta-value">${latest?formatDate(latest):'—'}</div></div></div><div class="customer-actions">${archived?`<button class="customer-action primary restore-action" onclick="restoreCustomer('${c.id}')">↩ ${ac('Restore Customer','استعادة العميل')}</button><button class="customer-action" onclick="openHistory('${c.id}')">📺 ${ac('View History','عرض السجل')}</button>`:`<button class="customer-action primary" onclick="openSubscriptionModal('${c.id}')">＋ ${ac('Add Subscription','إضافة اشتراك')}</button><button class="customer-action" onclick="openWhatsApp('${c.id}')">💬 ${ac('Open WhatsApp','فتح WhatsApp')}</button>`}</div></article>`}).join('')}
async function deleteCustomer(id){const c=allCustomersCache.find(x=>x.id===id);if(!c||!confirm(ac(`Archive ${customerFullName(c)}? Their old orders and subscription history will be preserved.`,`هل تريد أرشفة ${customerFullName(c)}؟ سيبقى سجل الطلبات والاشتراكات محفوظاً.`)))return;const{error}=await supabaseClient.from('customers').update({status:'archived'}).eq('id',id).eq('reseller_id',currentUser.id);if(error){alert(error.message||ac('Could not archive customer.','تعذر أرشفة العميل.'));return}await loadCustomers()}
async function restoreCustomer(id){const c=allCustomersCache.find(x=>x.id===id);if(!c)return;if(!confirm(ac(`Restore ${customerFullName(c)} to active customers?`,`هل تريد استعادة ${customerFullName(c)} إلى العملاء النشطين؟`)))return;const{error}=await supabaseClient.from('customers').update({status:'active'}).eq('id',id).eq('reseller_id',currentUser.id);if(error){alert(error.message||ac('Could not restore customer.','تعذر استعادة العميل.'));return}await loadCustomers()}
