/* Subly reseller notifications */
(function(){
  const style=document.createElement('style');
  style.textContent=`
  .topbar{overflow:visible}.subly-top-actions{display:flex;align-items:center;gap:10px}.notification-wrap{position:relative}.notification-btn{position:relative;width:42px;height:42px;border:1px solid var(--border);border-radius:11px;background:var(--card2);color:var(--text);cursor:pointer;font-size:19px;display:flex;align-items:center;justify-content:center}.notification-badge{position:absolute;right:-5px;top:-5px;min-width:20px;height:20px;padding:0 5px;border-radius:999px;background:var(--red);color:#fff;font-size:10px;font-weight:900;display:none;align-items:center;justify-content:center;border:2px solid var(--bg)}.notification-panel{position:absolute;right:0;top:52px;width:min(390px,calc(100vw - 24px));max-height:480px;overflow:auto;background:#121118;border:1px solid var(--border);border-radius:14px;box-shadow:0 22px 60px rgba(0,0,0,.48);z-index:2000;display:none}.notification-panel.show{display:block}.notification-head{position:sticky;top:0;background:#121118;border-bottom:1px solid var(--border);padding:13px 14px;display:flex;align-items:center;justify-content:space-between;gap:10px;z-index:2}.notification-head strong{font-size:14px}.notification-head button{border:0;background:transparent;color:#c9b9ff;font-size:11px;cursor:pointer}.notification-list{padding:8px}.notification-item{display:block;width:100%;text-align:left;border:0;border-radius:10px;padding:11px;background:transparent;color:var(--text);cursor:pointer}.notification-item:hover{background:#191720}.notification-item.unread{background:rgba(155,77,255,.09)}.notification-title{font-size:12px;font-weight:850}.notification-message{font-size:11px;line-height:1.45;color:#aaa7b3;margin-top:4px}.notification-time{font-size:9px;color:var(--muted);margin-top:6px}.notification-empty{padding:28px 16px;text-align:center;color:var(--muted);font-size:12px}@media(max-width:760px){.topbar{padding-left:14px;padding-right:14px}.online{display:none}.notification-panel{position:fixed;right:12px;top:70px;left:12px;width:auto;max-height:65vh}}
  `;
  document.head.appendChild(style);
  let notifications=[];
  async function wait(){for(let i=0;i<120;i++){if(window.currentUser||typeof currentUser!=='undefined'&&currentUser){init();return}await new Promise(r=>setTimeout(r,80));}}
  function inject(){const top=document.querySelector('.topbar');if(!top||document.getElementById('notificationButton'))return;const online=top.querySelector('.online');const wrap=document.createElement('div');wrap.className='subly-top-actions';wrap.innerHTML=`<div class="notification-wrap"><button id="notificationButton" class="notification-btn" type="button" aria-label="Notifications">🔔<span id="notificationBadge" class="notification-badge"></span></button><div id="notificationPanel" class="notification-panel"><div class="notification-head"><strong>Notifications</strong><button type="button" id="markAllNotifications">Mark all read</button></div><div id="notificationList" class="notification-list"><div class="notification-empty">Loading...</div></div></div></div>`;if(online){online.replaceWith(wrap);wrap.appendChild(online)}else top.appendChild(wrap);
    document.getElementById('notificationButton').addEventListener('click',e=>{e.stopPropagation();document.getElementById('notificationPanel').classList.toggle('show')});
    document.getElementById('markAllNotifications').addEventListener('click',markAllRead);
    document.addEventListener('click',e=>{if(!e.target.closest('.notification-wrap'))document.getElementById('notificationPanel')?.classList.remove('show')});
  }
  async function init(){inject();await loadNotifications();setInterval(loadNotifications,60000)}
  async function loadNotifications(){if(typeof currentUser==='undefined'||!currentUser)return;const{data,error}=await supabaseClient.from('notifications').select('id,type,title,message,link,is_read,created_at').eq('user_id',currentUser.id).order('created_at',{ascending:false}).limit(30);if(error){console.error('[SUBLY] notifications',error);return}notifications=data||[];render()}
  function render(){const list=document.getElementById('notificationList'),badge=document.getElementById('notificationBadge');if(!list||!badge)return;const unread=notifications.filter(x=>!x.is_read).length;badge.textContent=unread>99?'99+':String(unread);badge.style.display=unread?'flex':'none';if(!notifications.length){list.innerHTML='<div class="notification-empty">No notifications yet.</div>';return}list.innerHTML=notifications.map(n=>`<button class="notification-item ${n.is_read?'':'unread'}" type="button" data-id="${n.id}" data-link="${n.link||''}"><div class="notification-title">${escapeHtml(n.title)}</div><div class="notification-message">${escapeHtml(n.message)}</div><div class="notification-time">${formatDateTime(n.created_at)}</div></button>`).join('');list.querySelectorAll('.notification-item').forEach(b=>b.addEventListener('click',()=>openNotification(b.dataset.id,b.dataset.link)))}
  async function openNotification(id,link){await supabaseClient.from('notifications').update({is_read:true}).eq('id',id);if(link)location.href=link;else loadNotifications()}
  async function markAllRead(){if(typeof currentUser==='undefined'||!currentUser)return;await supabaseClient.from('notifications').update({is_read:true}).eq('user_id',currentUser.id).eq('is_read',false);await loadNotifications()}
  wait();
})();